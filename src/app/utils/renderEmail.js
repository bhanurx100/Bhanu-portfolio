// import { renderToString } from "react-dom/server";
// import EmailTemplate from "../components/EmailTemplate";

// export const renderEmail = ({ subject, message, email }) => {
//   return renderToString(<EmailTemplate subject={subject} message={message} email={email} />);
// };